\c airline_booking_db
You are now connected to database "airline_booking_db" as user "prabaa1".
set statement_mem = '200MB';
SET
select count(*) from airline_booking_test2;
 count 
-------
     0
(1 row)

select count(*) from airline_booking_test3;
 count 
-------
     0
(1 row)

select count(*) from airline_booking_t1heap;
 count 
-------
     5
(1 row)

\c template1
You are now connected to database "template1" as user "prabaa1".
select pg_sleep(5);
 pg_sleep 
----------
 
(1 row)

drop database airline_booking_db;
DROP DATABASE
