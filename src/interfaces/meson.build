# Copyright (c) 2022-2023, PostgreSQL Global Development Group

# NB: libpq is entered directly from the toplevel meson file
# Cloudberry does not build ecpg -- src/interfaces/Makefile has it commented
# out of SUBDIRS (only libpq is built). Keep the variable defined so the
# top-level meson.build can still reference it.
ecpg_targets = []
