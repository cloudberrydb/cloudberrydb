<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic
  PUBLIC "-//OASIS//DTD DITA Composite//EN" "ditabase.dtd">
<topic id="topic7">
   <title>Setting Parameters at the Database Level</title>
   <body>
      <p>Use <codeph>ALTER DATABASE</codeph> to set parameters at the database level. For
         example:</p>
      <p>
         <codeblock>=# ALTER DATABASE mydatabase SET search_path TO myschema;</codeblock>
      </p>
      <p>When you set a session parameter at the database level, every session that connects to that
         database uses that parameter setting. Settings at the database level override settings at
         the system level.</p>
   </body>
</topic>
