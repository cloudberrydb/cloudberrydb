platform: darwin
inputs:
  - name: xerces_patch
  - name: build_osx
outputs:
  - name: package_tarball_osx
run:
  path: xerces_patch/concourse/package_tarball.bash
params:
  src_root: build_osx/install
  dst_tarball: package_tarball_osx/bin_xerces_osx.tar.gz
