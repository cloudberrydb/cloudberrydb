platform: darwin
inputs:
  - name: orca_src
  - name: build_and_test_osx_release
outputs:
  - name: package_tarball_osx_release
run:
  path: orca_src/concourse/package_tarball.bash
params:
  src_root: build_and_test_osx_release/install
  dst_tarball: package_tarball_osx_release/bin_orca_osx_release.tar.gz
