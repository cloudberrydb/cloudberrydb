Oracle compatibility functions.

This package is documented in the `Oracle Compatibility Functions' section of
the Greenplum Database Administrator's Guide.

Docs
-------
http://gpdb.docs.pivotal.io/4360/utility_guide/orafce_ref.html

Compiling
------------
## Requires top-level configure to have been run.  Also requires pg_config to have
## been built and installed into $GPHOME (top-level configure --prefix).
PATH=$GPHOME/bin:$PATH make install USE_PGXS=1
